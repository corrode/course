# Word Count Challenge

This optional challenge combines strings, loops, and conditionals.
It does not count toward course completion.

Count words by their Unicode scalar values, not their byte lengths.
The exercise below has its own function and tests; it does not depend on code from another chapter.
