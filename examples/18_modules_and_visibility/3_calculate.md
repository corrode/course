# Making something reachable

This step doesn't compile. The compiler isn't being awkward; it's
enforcing the default that everything inside a module is private to
that module.

Hit Run, read the error, and make the smallest change that fixes
it. The error message names the exact item it's complaining about.
