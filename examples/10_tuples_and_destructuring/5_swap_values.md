# Swapping with Destructuring

Return the two values in reverse order, using tuple destructuring.

This tuple contains only `Copy` values. After the tests pass, try using the
original pair after passing it to `swap_values`. Why does that work, while
using the original name tuple after `get_first_name` does not?
