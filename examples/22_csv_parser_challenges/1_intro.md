# CSV Parser Challenges

Two optional tasks to try after the CSV parser.
Change the separator without breaking quoted fields, then reuse a parser through a small public API.

Each editor is independent and works in the browser or as a local Rust test file.
The module task includes a working line parser; it doesn't depend on finishing the delimiter task.
This bonus chapter does not count toward course completion.
