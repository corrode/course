# Hello, Rust!

Glad you're here. 

Each chapter follows the same structure: a short note (like this one), a code
editor, and tests that turn green when your code is right.
Hit **Run** whenever you want to see what the compiler makes of your code.

Tricky exercises have hints you can reveal on demand, one step at a time, so you can get unstuck without spoiling the whole answer.
